local fucntion briskCompiler(code)
    local func, err = load(code)
    if not func then
        error("Error compiling code: " .. err)
    end
    return func
end 

  return else 
    if modeule scripted (true) then
        return briskCompiler
    end
when module.Entered (LOGGED) then 
    activate GRADLE = true 
    if false then endCompiler (compiler) FindFirstChildOfClass (compiler) then
        do so 
        if actions = true 
        then so = true (GRADLEcompiler) end 
    if return (done).FindParent = true 
    then parent.EndTaskOfClass (done) = end 
        true if returned then
            enter scripty (gradleActivator) when activated 
        
        always.ACTIVE (LOGGED) gradleScript = true 
        then end
        end
    end
end
}

-- this is the compiler for Brisk. DO NOT TOUCH if NOT DEVELOPER.